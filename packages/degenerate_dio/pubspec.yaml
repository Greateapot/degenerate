name: degenerate_dio
description: Dio HTTP adapter for degenerate-generated OpenAPI Dart clients.
version: 0.4.1
repository: https://github.com/blopker/degenerate
resolution: workspace

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: ^3.8.0

dependencies:
  degenerate_runtime:
    git:
      url: https://github.com/Greateapot/degenerate.git
      ref: bugfix/typed-oneof
      path: packages/degenerate_runtime
  dio: ^5.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
